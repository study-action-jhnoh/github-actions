name: issue-workflow
on:
  issues:
    types: [opened]

jobs:
  push-job:
    runs-on: ubuntu-latest
    steps:
    - name: steps1
      run: echo hello world
    - name: steps2
      run: |
        echo hello world
        echo github action