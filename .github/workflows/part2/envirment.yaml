name: environment
on: push

jobs:
  get-env:
    runs-on: ubuntu-latest
    steps:
    - name: check env & secrets
      run: |
        echo ${{ vars.level }}
        echo ${{ secrets.key }}
  
  get-env-dev:
    runs-on: ubuntu-latest
    environment: critical
    steps:
    - name: check env & secrets
      run: |
        echo ${{ vars.level }}
        echo ${{ secrets.key }}